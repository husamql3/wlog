{
	"name": "@wlog/tui",
	"private": true,
	"type": "module",
	"bin": {
		"wlog": "./src/index.ts"
	},
	"scripts": {
		"dev": "bun run src/index.ts",
		"build": "bun build --compile --minify --sourcemap --bytecode src/index.ts --outfile wlog",
		"check-types": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@elysiajs/eden": "^1.4.9",
		"@wlog/server": "workspace:*",
		"commander": "^14.0.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@wlog/config": "workspace:*",
		"typescript": "catalog:"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
